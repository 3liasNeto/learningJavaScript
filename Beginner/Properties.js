/**
 * * Properties 
 * ? Each data type in javascript and general have yours particularities
 * ? and this is for all cases an string, numbers and objects can have the same
 * ? properties, some with value and others not theses values can be the length of
 * ? a string. So among these data types is
 * ? possible to have some properties.
 * 
 * * Methods
 */

/**
 * * Length 
 * * The Length of an 
 */